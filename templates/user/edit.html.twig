{% extends 'base.html.twig' %}

{% block title %}Modifier | Utilisateur{% endblock %}
       {% block body %}
        <div class="content-wrapper">
            <div class="row">
              <div class="col-12 grid-margin stretch-card">
                  <div class="card">
                    <div class="card-body">
                      <h4 class="card-title" style="margin-left:35%">Modification d'un utilisateur</h4>
                      
                      {{ include('user/_form.html.twig', {'label': 'Modifier'}) }}
                    </div>
                  </div>
                </div>
            </div>

        </div>
        {% endblock %}