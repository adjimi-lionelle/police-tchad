{% extends 'base.html.twig' %}

{% block title %}Modifier | Candidats{% endblock %}
       {% block body %}
        <div class="content-wrapper">
            <div class="row">
              <div class="col-12 grid-margin stretch-card">
                  <div class="card">
                    <div class="card-body">
                      <h4 class="card-title" style="margin-left:35%">Modification d'un candidat</h4>
                      
                      {{ include('candidat/_form.html.twig', {'button_label': 'Modifier'}) }}
                    </div>
                  </div>
                </div>
            </div>

        </div>
        {% endblock %}